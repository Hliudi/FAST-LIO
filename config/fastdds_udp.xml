<?xml version="1.0" encoding="UTF-8" ?>
<!-- Force Fast DDS to use UDPv4 only (disable shared-memory transport).
     Some platforms (WSL2, certain ARM boards incl. RK3588) have flaky SHM,
     which shows up as topics that "exist" but never deliver data.
     Enable with:
        export FASTRTPS_DEFAULT_PROFILES_FILE=<repo>/config/fastdds_udp.xml
     before launching. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_only</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>
  <participant profile_name="udp_only_profile" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_only</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
